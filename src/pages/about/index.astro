---

import PageLayout from "@layouts/PageLayout.astro";
import Container from "@components/Container.astro";
import BackToPrev from "@components/BackToPrev.astro";
import Link from "@components/Link.astro";
import { SITE, SOCIALS } from "@consts";

---

<PageLayout title="About" description="About Davide">
    <Container>
        <div>
            <BackToPrev href="/">
                Back home
            </BackToPrev>
        </div>
        <div class="space-y-10">
            <div class="my-6 text-center pb-5 border-b-2 border-[var(--rule-strong)]">
                <div class="mono-label mono-label-accent mb-3">
                    About — the person
                </div>
                <h1 class="font-display text-4xl font-medium tracking-tight text-[var(--text-primary)] mb-3">Who am I</h1>
                <div class="text-[var(--text-primary)]">
                    Infos about me
                </div>
            </div> 

            <!-- What -->
            <section class="mb-8 border-t border-[var(--border)] pt-6">
                <h2 class="mono-label text-[12px] mono-label-accent mb-2">What</h2>
                <p class="text-[var(--text-primary)]">
                    Today, I'm a full-time Software Engineer for a Swiss company that operates in the management of the Workforce. <br>
                    Before this, I studied @ University Of Pisa while I maintained an active role in the Developer community via the GDG Pisa.
                </p>
            </section>

            <!-- How -->
            <section class="mb-8 border-t border-[var(--border)] pt-6">
                <h2 class="mono-label text-[12px] mono-label-accent mb-2">How</h2>
                <p class="text-[var(--text-primary)]">
                   I bring curiosity to everything I touch,<br>
                   often finding new angles on things that seem perfectly defined to everyone else.
                </p>
            </section>

            <!-- Why -->
            <section class="mb-8 border-t border-[var(--border)] pt-6">
                <h2 class="mono-label text-[12px] mono-label-accent mb-2">Why</h2>
                <p class="text-[var(--text-primary)]">
                   I'm a builder by nature.<br>
                   I can't help but wonder why certain things don't exist yet—and that restless curiosity pushes me to dive into new areas of learning and discovery every single day.
                </p>
            </section>

            <!-- Where -->
            <section class="mb-8 border-t border-[var(--border)] pt-6">
                <h2 class="mono-label text-[12px] mono-label-accent mb-2">Where</h2>
                <p class="text-[var(--text-primary)] mb-2">
                    Feel free to reach out—whether it's just to chat, collaborate, or share ideas! 
                </p>
                <div class="space-y-6">
                    <div class="flex flex-wrap items-center gap-4 text-sm">
                        {SOCIALS.map((social, index) => (
                            <span class="flex items-center gap-2">
                                <Link
                                    href={social.HREF}
                                    external
                                    aria-label={`${SITE.NAME} on ${social.NAME}`}
                                >
                                    {social.NAME}
                                </Link>
                                {index < SOCIALS.length - 1 && (
                                    <span class="text-[var(--border-strong)]">/</span>
                                )}
                            </span>
                        ))}
                        <span class="flex items-center gap-2">
                            <span class="text-[var(--border-strong)]">/</span>
                            <Link
                                href={`mailto:${SITE.EMAIL}`}
                                aria-label={`Email ${SITE.NAME}`}
                            >
                                {SITE.EMAIL}
                            </Link>
                        </span>
                    </div>
                </div>
            </section>
        </div>
    </Container>
</PageLayout>
